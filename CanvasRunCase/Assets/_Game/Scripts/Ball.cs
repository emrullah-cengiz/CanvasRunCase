using Assets.Scripts;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ball : MonoBehaviour
{
    //public Ball frontBall;
    public GameObject mesh;

    //private void Update()
    //{
    //    //new Vector3(
    //    //    Mathf.Lerp(transform.position.x, frontBall.transform.position.x, Time.deltaTime * 80),
    //    //    Mathf.Lerp(transform.position.y, frontBall.transform.position.y, Time.deltaTime * 80),
    //    //    Mathf.Lerp(transform.position.z, frontBall.transform.position.z + mesh.transform.localScale.x, Time.deltaTime * 80))
    //    //if (frontBall != null)
    //    //    transform.SetPositionAndRotation(Vector3.Lerp(transform.position, transform.forward - Vector3.back * mesh.transform.localScale.x, Time.deltaTime * 80),
    //    //        Quaternion.Lerp(transform.rotation, transform.rotation, Time.deltaTime * 80));

    //    }
    }
